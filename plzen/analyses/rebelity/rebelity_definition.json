{
  "since": "2022-10-18",
  "until": null,
  "present_options": ["yes", "no", "abstain"],
  "absent_options": ["absent", "not voting"],
  "yes_options": ["yes"],
  "no_options": ["no"],
  "extras": {
    "approval_status": "DRAFT — measures alignment with each person's own REAL klub (plzen:org:group:* rows from plzen/scripts/party_affiliation.py, derived from per-vote klub text captured directly in each protocol — D7's preferred live-group case, same standing as Brno/Ostrava/Most). Does not depend on which groups form the government, so it does not require the same D7 sign-off as govity/wpca's government_groups — ready to use as-is.",
    "vocabulary_source": "Same vocabulary mapping as attendance_definition.json.",
    "note": "Rebelity requires each person's group membership at each vote-event date, sourced from data/memberships.csv's plzen:org:group:* rows (dated intervals derived via contiguous-run detection over the MODE klub per meeting — see party_affiliation.py's module docstring) via the all-members-shaped persons file the runner builds.",
    "known_data_quality_notes": "party_affiliation.py merged one confirmed pure system-relabeling artifact (era1's 'STAROSTOVÉ A NEZÁVISLÍ' -> era2/3's 'STAN', same entity, verified via continuous per-person date boundaries with zero gap) but deliberately did NOT merge era1's combined 'ODS, KDU-ČSL, TOP 09' list into its era2/3 successors -- that one is a real 3-way split (9/3/3 people landing in ODS/KDU-ČSL/TOP 09 respectively, matching the real 'SPOLU' coalition's reported dissolution) -- so rebelity scores for era1 votes will show these 15 people under the combined list's identity, not their later individual party."
  }
}
